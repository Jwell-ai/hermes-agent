services:
  alphart-agent:
    build:
      context: .
      dockerfile: Dockerfile.alphart
    image: alphart-hermes-agent:latest
    restart: unless-stopped
    env_file:
      - ./.env.alphart
    ports:
      - "58088:58088"
