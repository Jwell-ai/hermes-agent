services:
  canvas-agent:
    build:
      context: .
      dockerfile: Dockerfile.canvas
    image: canvas-hermes-agent:latest
    restart: unless-stopped
    env_file:
      - ./.env.canvas
    ports:
      - "58088:58088"
